{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the local Ship Check review surface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
