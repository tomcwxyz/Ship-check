# Desktop engine resource

Release builds place the platform-native trusted Ship Check engine in this directory as `ship-check-engine` on Linux/macOS or `ship-check-engine.exe` on Windows before Tauri packaging.

Generated engine binaries are release artefacts and are not committed to the repository.
